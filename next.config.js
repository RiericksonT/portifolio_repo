/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  images: {
    domains: ['baudotiozao.files.wordpress.com',"avatars.githubusercontent.com"],
  },
}

module.exports = nextConfig
